# 다음 세션 시작 프롬프트

> 이 파일 내용을 다음 세션 시작 시 그대로 붙여넣어 쓴다. 배경은 `doc/m1-live-findings.md`
> §27~28에 전부 기록돼 있으니, 이 프롬프트는 "무엇을 할지"만 순서대로 정리한 것이다.

---

`band-score-capture` 프로젝트를 이어서 진행한다. 먼저 `CLAUDE.md` 전체와
`doc/m1-live-findings.md` §27~28을 읽어라.

## 지금 상태 요약 — M1 acquire 핵심 검증은 끝났다

§27-4에서 계획한 세 가지 수정(멤버 목록 스크롤, 탭 전환 로딩 대기, 이벤트버스 중복소비)과
§28에서 실기동 중 추가로 발견한 멤버 검증 로직 버그들(탭 언어 의존성, 멤버 목록 지연 로딩,
`goBack()` DOM 리셋)까지 전부 구현·수정하고, **57명 실전 스케일로 최종 검증까지 완료했다**:

- 이벤트버스 중복소비: 0건(전체 스케일)
- 멤버별 댓글 수 교차검증: 57/57명 확보(전체 스케일)
- 멤버별 대조 불일치: 단 2명(그마저도 이번 실행의 게시글 부분 수집 때문 - 버그 아님)

`acquire/collector.js`는 이 상태로 커밋 전이다(사용자 요청 없어서 커밋 안 함) - `git diff`로
전체 변경 사항 확인 가능.

## 할 일

1. **커밋 여부를 먼저 사용자에게 물어볼 것.** 지난 세션에서 만든 변경 사항이 크고
   (member-verify 로직 재설계 2회 포함), 아직 한 번도 커밋 안 됐다.
2. §28-7에서 발견된 "게시글 순회가 평소 53~61건 대신 14건에서 조기 중단"되는 산발적 이슈가
   재현되는지 관찰. 한 번만 나온 거라 아직 근본 원인 조사 단계는 아니지만, 또 나오면
   `logs/trace/*.ndjson`의 `postDetail`/`clickNextPost` 관련 엔트리로 진단할 것.
3. 위 두 가지가 문제없으면 **M1 acquire를 완료 선언**하고 scoring(M2) 설계로 넘어갈 수 있다.
   `doc/PLAN.md`/`doc/project_brief.md` 참고.

## 원칙 (이번 세션에서 확인된 것)

- CDP 원격 디버깅이나 `node_modules/.bin/electron <스크립트>` 같은 가벼운 타겟 테스트 스크립트로
  먼저 가설을 검증하고, 확신이 서면 전체 사이클을 최소 횟수로 도는 방식이 효과적이었다(§28-6).
- `win.loadURL()`을 직접 쓰지 말고 항상 `safeLoadURL`류 래퍼(`ERR_ABORTED` 또는 `(-3)` 코드
  둘 다 무시)를 쓸 것 - 이 환경에서 에러 메시지에 "ERR_ABORTED" 텍스트가 없는 경우가 실측
  확인됐다(§28-5).
- 밴드 UI 언어(한국어/영어)는 세션마다 바뀔 수 있다 - **텍스트 매칭에 의존하지 말 것**
  (§28-3). 위치/순서/class 기반으로 selector를 짤 것.
- 서버 응답이 `status:200`인데 `bodyLen:0`이고 비정상적으로 느린 패턴을 한 번 관측했지만
  (§28-6), 바로 재시도했더니 문제없이 재현 안 됐다(§28-7) - 재현성 없는 1회성 신호는
  과도하게 확대 해석하지 말 것.
